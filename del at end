#include<stdio.h>
int main() {
    int a[10],n,i;
    printf("enter array size : ");
    scanf("%d",&n);
    printf("enter array elements are :");
    for(i=0;i<n;i++)
    scanf("%d",&a[i]);
    printf("after deletion array is :");
    for(i=0;i<n-1;i++)
    printf("%d",a[i]);
    return 0;
}
